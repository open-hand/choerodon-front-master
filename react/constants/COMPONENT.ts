/**
 * 组件默认值
 * @description 避免提示 propType "xxx" is not required, but has no corresponding defaultProps declaration.
 * 默认都是 undefined
 */
export const COMPONENT_DEFAULT_PROPS = {} as Record<string, any>;
